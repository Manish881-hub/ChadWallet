# @privy-io/react-auth has peerOptional deps (@solana-program/*, @stripe/crypto)
# that create irreconcilable version conflicts with @solana/kit (v5 vs v6).
# Since these are optional and unused by our app, skip strict peer dep resolution.
legacy-peer-deps=true
