// Empty module stub — used by next.config.ts NormalModuleReplacementPlugin
// to satisfy broken @solana/kit subpath imports from transitive dependencies
// (e.g. @solana-program/system trying to import @solana/kit/program-client-core).
// These code paths are never executed in our app.
module.exports = {};
